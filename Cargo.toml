[package]
name = "ffmpeg-tui"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.44"
serde = { version = "1.0", features = ["derive"] }
[dependencies.uuid]
version = "1.23.0"
# Lets you generate random UUIDs
features = [
    "v4",
]